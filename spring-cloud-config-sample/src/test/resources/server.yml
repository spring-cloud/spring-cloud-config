info:
  component: Config Server
spring:
  cloud:
    config:
      server:
        basedir: target/config
  application:
    name: configserver
  jmx:
    default_domain: cloud.config.server
server:
  port: 8888
management:
  contextPath: /admin