info:
  component: Config Server
spring:
  cloud:
    config:
      server:
        git:
          basedir: target/config
  application:
    name: configserver
  jmx:
    default_domain: cloud.config.server
server:
  port: 8888
management:
  contextPath: /admin
